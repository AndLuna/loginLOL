/* importando uma fonte para o projeto */
@import url('https://fonts.googleapis.com/css2?family=Lexend+Deca:wght@300;400;500;600;700&display=swap');
/* eliminando toda a configuracao previa que o navegador pode ter */

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Lexend Deca', sans-serif;
}

/* chamo a pseudoclasse root */
:root {
  /* e crio VARIAVEIS pra armazenar as cores que irei utilizar*/
  --white: #fff;
  --whitesmoke: #f9f9f9;
  --light-grey: #ececec;
  --grey: #c4c4c4;
  --medium-grey: #a7a7a7;
  --deep-grey: #343434;
  --black: #000000;
  --blue: #1b77f3;
  --red: #cf3c3f;


}

/* configurando as dimensoes do login */
.login {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  max-width: 500px;
  min-height: 100vh;
  /* ao esticar a tela: definindo um pad dinamico que nao ultrapasse um limite - minimo, desejavel e maximo */
  padding: clamp(35px, 8%, 75px);
  /* background-color: #999; */
  /* utilizando o background-color: #999 eu consigo enxergar as dimensoes da coluna de Login  */
  
}

.login__logo {
  width: 130px;
  margin-bottom: -200px;
}

.login__title {
  color: var(--deep-grey);
  /* utilizei o nome da variavel aqui  */
  font-size: 1.5em;
  margin-bottom: 30px;
  margin-top: 0px;
}

.login__label { 
  /* LOGIN LABEL É A BARRINHA ///background-color: red;  caso eu quisesse por um background na label de loguin*/
  display: black;
  margin-bottom: 20px;
  width: 100%;
  margin-top: -270px;

}

.input {
  /* input é as frases na barrinha / como se fosse uma subclasse de login label */
  width: 100%;  /*largura da barrinha de nome de usuario */
  background-color: var(--light-grey);
  border: 2px solid rgba(0, 0, 0, 0); /* mechendo na borda do input pra tirar o movimento que empurra a borda */
  border-radius: 5px;
  outline: none;  /* serve para tirar a borda azul de selecionado */
  font-size: 0.9em; /* tamanho da fonte para tudo que eu digitar nesse input  */
  padding: 25px 10px 10px;   /* dimensoes na barra de imput  */
  font-weight: 600;  /* deixa a letra mais escura*/
  color:var(--deep-grey);
}

.login__label span {
  position:absolute;
  font-size: 0.8em;
  font-weight: 700;
  text-transform:uppercase;
  color: var(--medium-grey) ;
  margin: 20px;
  cursor: text;
  transition: all 200ms ease;
  
  }

  .input:focus {
    /* quando o input estiver focado */
     background-color: whitesmoke; /* quando for clikado o input mudará de cor */
     border: 2px solid var(--deep-grey);  
  }
/* resolve o problema da frase ficar em cima do nome de usuario */
  .login__label .span-active{
    font-size: 0.7em;
    margin: 8px 10px;

  }

/* mexendo com os icones da pagina */
  .login__icons{
     display: flex;
     gap: 10px; /*distancia entre os icones */
    width: 100%;  /*centralização */
    margin-top: -100px; /*separar os icones dos inputs de loguin */
    margin-bottom: ;
    background-color: #1b77f3
  }

  .login__icons img{
    width: 20px;
  }

  .icons__button{
    width: 33.33%
  }